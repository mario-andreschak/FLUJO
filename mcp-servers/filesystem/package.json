{
  "name": "@mario.andreschak/mcp-filesystem",
  "version": "3.41.0",
  "description": "FLUJO's standalone confined filesystem MCP server",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "flujo-mcp-filesystem": "dist/index.js"
  },
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node ../embed-shared.mjs filesystem",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@flujo-ai/mcp-shared": "0.1.0",
    "@types/node": "^20.17.30",
    "typescript": "5.8.3"
  }
}
