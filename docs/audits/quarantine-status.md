# Test quarantine review — 2026-09-16

Owners below are subsystem maintainer roles, not assignments to invented individuals. Quarantine retirement requires new results from the stabilization checkout; the prior green run is supporting evidence only. A quarantined suite must still execute, and newly skipped tests or startup/parse failures are blocking under the revised baseline gate. All nine exemptions have now been removed after fresh passing results; no active quarantine remains.

Historical evidence: [verify run 34763333121](https://github.com/mario-andreschak/FLUJO/actions/runs/34763333121), revision `26cd39856fd8a5580c77025ee0ec95732572045d`. Its raw main Jest artifact contains eight passing quarantined suites (109 passing assertions) and one failing quarantined suite (two assertions). The old baseline recorded no per-suite reason or exit criteria; an earlier failure cause cannot be inferred from a current pass.

Fresh evidence: the 2026-09-16 Windows remediation checkout's `jest-results.json` records 761 passing, three failing and four intentionally skipped suites, with 6,884 passing, seven failing and six intentionally skipped assertions. All seven retired exemptions below pass in that complete main run (98 assertions). The separate `jest-results-followup.json` records five passing suites and 33 assertions after correcting the remote-root storage fixture and ending concurrent build/soak load. These are local validation artifacts under artifact-relative `remediation/` (raw files retained locally, outside this repository); the main run itself was not wholly green.

The complete isolated stage then passed all nine suites and 101 assertions, with zero failures or skips (`jest-results-isolated.json` and `validation-tests-isolated.log` in the same artifact directory). This includes capture/recording (2/2), snapshot Git integration (11/11) and memory lifecycle (9/9), meeting the final two quarantine exit conditions. No test timeout or performance threshold was weakened. The final complete main rerun passed **763 suites and 6,874 assertions**, with four skipped suites containing six explicitly allowed opt-in assertions, in 512.644 seconds (`jest-results-final.json`). Both strengthened baseline gates passed with zero quarantines. Subsequent narrowly scoped environment-secret and installer fixes receive separate targeted verification in the [remediation record](2026-09-16-remediation.md).

| Suite under `__tests__/` | Accountable subsystem | Verified evidence and present reason | Exit condition |
| --- | --- | --- | --- |
| `chat/conversationContentSearch.test.ts` | Conversation storage/API maintainers | Fresh main run: 19/19 passed. Exemption removed. | Met; future failures block. |
| `mcp/browserCaptureRecording.test.ts` | Browser MCP and CI maintainers | Historical MCP startup failure traced to an absent compiled entry. Moved to the serial isolated job, which builds the MCP artifacts first; captured startup stderr and ensured failure cleanup. Fresh complete isolated run: 2/2 passed. Exemption removed. | Met; compiled-artifact prerequisites and both process assertions remain blocking. |
| `mcp/browserAudioStream.test.ts` | Browser MCP maintainers | Fresh main run: 1/1 real-browser/audio assertion passed. This suite imports source and does not depend on the absent compiled entry. Exemption removed. | Met; browser prerequisites and the assertion remain blocking. |
| `workspace/routeCoverage.test.ts` | Workspace isolation and HTTP API maintainers | Fresh main run: 1/1 route-classification assertion passed. Exemption removed. | Met; every future route must remain explicitly classified. |
| `snapshot/ShadowRepoService.test.ts` | Snapshot/Git integration maintainers | Fresh main run: 8/11 passed; three exceeded 15 seconds under concurrent build/soak load. Serial followup and complete isolated stage both passed 11/11 with unchanged assertions/timeouts. Moved to the isolated job to prevent contention with parallel main tests. Exemption removed. | Met; Git, isolated temporary repositories and all 11 assertions remain blocking. |
| `frontend/components/ExperimentalFeatures.test.tsx` | Frontend navigation/settings maintainers | Fresh main run: 22/22 component assertions passed, including current setup/navigation changes. Exemption removed. | Met; future failures block. |
| `enduringAgents/activityRouting.test.ts` | Persona runtime/routing maintainers | Fresh main run: 9/9 routing/lease assertions passed. Exemption removed. | Met; investigate any future lease race instead of retaining a whole-suite waiver. |
| `enduringAgents/personaDispatcher.test.ts` | Persona dispatcher maintainers | Fresh main run: 45/45 dispatch assertions passed. Exemption removed. | Met; any future timing issue needs an exact failing assertion, diagnosis and passing correction. |
| `enduringAgents/runtimeEventsAppendScaling.test.ts` | Persona event-store/performance maintainers | Fresh main run: 1/1 assertion passed, including 20,000 appends, deterministic parse/cache/memory checks and the secondary p95 ratio. Exemption removed. | Met; deterministic checks and measured timing contract remain blocking. |

The capture/recording move transfers one suite and two assertions. The snapshot Git and memory lifecycle moves transfer another two suites and 20 assertions (11 and nine respectively, verified in raw main, serial followup and isolated results). Memory lifecycle's 28-day steady-state assertion also exceeded 15 seconds under concurrent build/soak load; its complete suite passed both serial runs with unchanged assertions/timeouts. Both intensive integration suites now run in the isolated serial job. Initially the transfers preserved the historical combined 767-suite/6,876-assertion floor. The reviewed stronger floors below include the restored/new coverage.

After the transfers, the reviewed main/followup union contains 763 executed main suites and 6,874 executed assertions on Windows, including the new three-assertion model navigation suite. The portable main floor is now **762 suites / 6,869 assertions**: five Windows-only assertions are intentionally skipped on Linux/macOS, three of them comprising the entire `bashNpmRunEnv` suite. This derivation is not a claim that the full Linux scope has been rerun. The complete passing isolated result sets its floor at **nine suites / 101 assertions**. The combined portable floor is **771 suites / 6,970 assertions**, raised from the historical 767/6,876.

No exemption should remain indefinitely on the strength of a historic label such as “flaky.” For each new failure, record the exact failing assertion, reproduction conditions, owner subsystem, corrective change and passing exit evidence here before extending a waiver. Remove rows from the active waiver list in `test-baseline.json` when their exit condition is met; retain this document as the audit trail.
